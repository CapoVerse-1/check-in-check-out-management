import React from 'react';
import { Container, Alert } from 'react-bootstrap';

const ImportCSV = () => {
  return (
    <Container className="mt-4">
      <h2>Import CSV</h2>
      <Alert variant="info">
        This is a placeholder for the CSV Import component.
      </Alert>
    </Container>
  );
};

export default ImportCSV; 